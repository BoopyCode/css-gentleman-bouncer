# CSS Gentleman Bouncer 🎩

Finds duplicate CSS class declarations that are fighting for attention in your stylesheets. Run: `node css-gentleman-bouncer.js your-styles.css` to see which classes need to be escorted out of the club.